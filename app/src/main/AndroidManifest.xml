<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.projectchisel">

    <dist:module dist:instant="true" />

    <uses-feature android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Homepage.Homepage" android:launchMode="singleTop"></activity>
        <activity android:name=".Settings.Settings" android:launchMode="singleTop"></activity>
        <activity android:name=".Profile.Profile" android:launchMode="singleTop"></activity>
        <activity android:name=".AddPost.AddPost"></activity>
        <activity android:name=".Explore.Explore" android:launchMode="singleTop"></activity>
        <activity android:name=".AddPost.AddWorkout" android:launchMode="singleTop"></activity>
        <activity android:name=".AddPost.AddRecipe" android:launchMode="singleTop"></activity>
    </application>

</manifest>